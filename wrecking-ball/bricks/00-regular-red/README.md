# Regular Red brick, variable width

This tileset allows to generate bricks with a width as large as wanted.

The naming scheme is binary :

* The bit on the left is clear when the left end is opened.
* The bit on the right is clear when the right end is opened.
