# Regular Orange brick, variable width

This tileset allows to generate bricks with a width as large as wanted.

The naming scheme is binary : `{left_end}{right_end}`

* `{left_end}` : the bit is clear when the left end is opened.
* `{right_end}` : the bit is clear when the right end is opened.

